apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azure-billing-exporter.fullname" . }}-test-connection"
  labels:
{{ include "azure-billing-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "azure-billing-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never